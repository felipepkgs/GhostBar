enum AppVersion {
    /// Bump alongside the git tag on each release (see README's release
    /// flow) — UpdateChecker compares this against the latest GitHub tag.
    static let current = "0.1.0"
}
